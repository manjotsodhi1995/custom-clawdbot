version: '3.8'

services:
  clawdbot:
    build: .
    container_name: clawdbot-gateway
    ports:
      - "18789:18789"
    volumes:
      - clawdbot-config:/home/clawdbot/.clawdbot
    restart: unless-stopped

volumes:
  clawdbot-config:
